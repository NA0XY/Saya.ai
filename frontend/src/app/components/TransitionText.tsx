export function TransitionText() {
  return (
    <section className="w-full flex items-center justify-center" style={{ padding: "100px 80px" }}>
      <h2
        className="heading-display text-center text-[#E85D2A]"
        style={{ fontSize: "clamp(32px, 3vw, 52px)" }}
      >
        THERE HAS TO BE A BETTER WAY.
      </h2>
    </section>
  );
}
